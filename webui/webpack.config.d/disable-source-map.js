// Disable source-map generation in production builds to reduce output size.
// The auto-generated webpack.config.js sets devtool = 'source-map'; we override
// it here so that .map files are not emitted.
config.devtool = false;
